# 端午这天，我们做了一款「最不像游戏的游戏」

> 五四青年团队 · 23物流2班 · 端午文化公益献礼

---

## 一、一个跨界的开始

大家好，我们是23物流2班的"五四青年团队"。

你可能想不到，一个学物流的团队，会去做一款端午文化游戏。

其实我们自己也觉得"离谱"。我们这个团队，过去做的项目都是"正经"的物流管理类——"物流之声"宣传系统、"供应链啤酒游戏"实训平台，都是和本专业强相关的东西。老师夸我们"专业扎实"，同学说我们"很稳"。

但今年端午前夕，团队开会的时候，有个同学突然说了句：

> "我们能不能做一个不那'正经'的东西？一个能让大家玩着玩着，就记住端午节文化的东西？"

全场安静了三秒，然后全票通过。

就这样，五个学物流的小伙伴，开始了为期一周的端午文化探险游戏开发。

我们给自己定了一个很"轴"的目标：

**要让一个完全不了解端午的年轻人，打开这个游戏，玩上三分钟，就能记住屈原是谁、为什么要赛龙舟、粽子有哪些种类。**

不是为了考试，不是为了应付作业——是真心觉得，我们Z世代应该有自己讲文化的方式。

---

## 二、为什么是「游戏」，而不是 H5 或推文？

这个问题我们讨论了很久。

公众号推文？写过了，大家划过去就算完。
短视频？拍过了，端午那几天全是包粽子的，淹没在信息流里。
H5 长图？做过，阅读完成率不到 8%。

最后我们选了游戏——一个需要"动手"才能完成的小游戏。

为什么？因为我们这一代人，对"参与"的要求比上一代高得多。我们不想只是"看"端午，我们想"玩"端午。

于是，端午文化探险 H5，诞生了。

---

## 三、三关怎么玩？我们想讲什么故事

游戏分三关，每一关对应端午的一个核心文化点。

### 第一关：击鼓赛舟
玩家点击大鼓，跟随节奏敲击。这是致敬"赛龙舟"的鼓手——龙舟能不能赢，全看鼓手节奏稳不稳。我们把鼓做成了红木雕花配朱砂鼓钉，点击有 3 层鼓声叠加（鼓面音、鼓边音、鼓腔共振），就是为了那个"咚——嚓——咚嚓"的灵魂感。有同学玩完后跟我们说：

> "我在地铁上玩，路过的人都在看我手机，我都不好意思了。"

这大概就是物理传播吧。

### 第二关：端午知识问答
屈原会突然插话问你："端午节最早起源于什么？"、"《离骚》的作者是谁？"。我们准备了 18 道题，涵盖了端午的起源、习俗、诗词、食物等。如果答错了，屈原会叹口气说："后生可畏，但还是要多读书啊……"——不是冷冰冰的"回答错误"，而是有温度的、像爷爷跟孙子说话的口吻。

### 第三关：投粽入江
龙舟在江面上飞驰，玩家要把粽子准确地"投"进江里给屈原。这关的难点是——江里有鱼会乱游，干扰你的视线。所以我们给鱼做了不同的方向，有些鱼向左游、有些鱼向右游（这是个 bug 改了三遍才修好的故事，下面会讲）。

通关后，玩家会得到一个"文化达人"的称号，并能看到一个国潮风的分享海报——这才是真正的"晒成绩"时刻。

---

## 四、技术踩过的那些坑（写给同样想做点什么的你）

我们五个人里，没有人系统学过前端开发。HTML 是自学的，CSS 是看 W3School 学的，JavaScript 是看 MDN 学的。所以这一周，我们踩了很多很多坑。

### 坑 1：白屏

第一次部署到 GitHub Pages 的时候，打开是白屏。我们以为是服务器问题，结果排查了三个小时，发现是 JavaScript 里的字符串换行符不对——一个 `"` 后面跟了换行，导致整个脚本解析失败。

出问题的代码长这样（`opt:["` 后被换行符"截断"了）：

```javascript
// ❌ 出错的写法（"[" 后面无意中换行）
const quiz = {
  q: "端午节最早起源于?",
  opt:[
    "纪念屈原",
    "吴越图腾"
  ],
  ans: 0
}
```

正确的写法是用 `+` 拼接，或者直接写在同一行：

```javascript
// ✅ 修复后
const quiz = {
  q: "端午节最早起源于?",
  opt: ["纪念屈原", "吴越图腾"],
  ans: 0
}
```

修复就一行代码，但找这个 bug 找了一晚上。

**教训：本地一定要在浏览器里实测，不要相信"看起来对"。**

### 坑 2：鱼倒着游

第三关里，鱼是从屏幕右边往左边游的。但 emoji 鱼（🐟）默认是头朝左的，所以如果我们不做任何处理，鱼其实是"倒退"的。看起来特别诡异。

我们一开始用 `transform: scaleX(-1)` 来翻转，结果发现有些浏览器不兼容。最后用了最笨的方法——给每条鱼加一个 `.fish-right` class，根据它要游的方向，决定要不要翻转。

```html
<!-- HTML：所有鱼先按"向左游"渲染 -->
<span class="fish">🐟</span>
<span class="fish">🐟</span>
<span class="fish fish-right">🐟</span>
```

```css
/* CSS：根据 class 决定要不要水平翻转 */
.fish {
  display: inline-block;
  font-size: 32px;
  animation: swim-left 8s linear infinite;
}

/* 向右游的鱼：用 class 翻转一次 */
.fish-right {
  transform: scaleX(-1);
  animation-name: swim-right;
}

@keyframes swim-left  {
  from { transform: translateX(100vw); }
  to   { transform: translateX(-100px); }
}
@keyframes swim-right {
  from { transform: scaleX(-1) translateX(-100px); }
  to   { transform: scaleX(-1) translateX(100vw); }
}
```

```javascript
// JS：生成鱼的时候，50% 概率给 .fish-right
function spawnFish() {
  const el = document.createElement('span');
  el.className = 'fish' + (Math.random() < 0.5 ? ' fish-right' : '');
  el.textContent = '🐟';
  document.querySelector('#river').appendChild(el);
}
```

**教训：emoji 在不同系统上的渲染可能不一样，能用 CSS 控制的，就不要赌 emoji 本身。**

### 坑 3：鼓声的"灵魂"

第一关的鼓声，我们一开始用的是现成的 MP3。听起来像是有人在敲塑料桶。

后来我们改用 Web Audio API 自己合成——把鼓声拆成"鼓面音"（高频短促）、"鼓边音"（中频清脆）、"鼓腔共振"（低频浑厚）三个层次，根据点击力度实时叠加。听起来——

就像真鼓了。

核心合成代码：

```javascript
// 1. 懒加载 AudioContext（必须等用户第一次手势才能创建）
let ctx = null;
function getCtx() {
  if (!ctx) ctx = new (window.AudioContext || window.webkitAudioContext)();
  if (ctx.state === 'suspended') ctx.resume();
  return ctx;
}
document.addEventListener('click', getCtx, { once: true });

// 2. 鼓声三件套：鼓面 + 鼓边 + 鼓腔共振
function playDrum(force = 1) {
  const ac = getCtx();
  const t = ac.currentTime;

  // 鼓面音：高频短促（"咚"的清脆头）
  const face = ac.createOscillator();
  face.type = 'triangle';
  face.frequency.setValueAtTime(220, t);
  face.frequency.exponentialRampToValueAtTime(60, t + 0.08);
  const faceGain = ac.createGain();
  faceGain.gain.setValueAtTime(0.6 * force, t);
  faceGain.gain.exponentialRampToValueAtTime(0.001, t + 0.15);
  face.connect(faceGain).connect(ac.destination);
  face.start(t); face.stop(t + 0.15);

  // 鼓边音：中频清脆（"嚓"的金属感）
  const rim = ac.createOscillator();
  rim.type = 'square';
  rim.frequency.setValueAtTime(1800, t);
  const rimGain = ac.createGain();
  rimGain.gain.setValueAtTime(0.15 * force, t);
  rimGain.gain.exponentialRampToValueAtTime(0.001, t + 0.05);
  rim.connect(rimGain).connect(ac.destination);
  rim.start(t); rim.stop(t + 0.05);

  // 鼓腔共振：低频浑厚（"嗡"的胸腔感）
  const body = ac.createOscillator();
  body.type = 'sine';
  body.frequency.setValueAtTime(80, t);
  const bodyGain = ac.createGain();
  bodyGain.gain.setValueAtTime(0.4 * force, t);
  bodyGain.gain.exponentialRampToValueAtTime(0.001, t + 0.4);
  body.connect(bodyGain).connect(ac.destination);
  body.start(t); body.stop(t + 0.4);
}

// 3. 点击鼓面时调用
document.querySelector('#drum').addEventListener('click', () => {
  playDrum(1.0);
});
```

**教训：声音是游戏的灵魂，不要用现成的 MP3 糊弄。**

### 坑 4：排行榜怎么做？

我们想做排行榜，让全班同学能看到彼此的分数。一开始想用后端，但我们五个人里没有会后端的。

最后找到一个叫 JSONBin 的服务——一个免费的云端 JSON 存储。我们把前 50 名玩家的成绩存到云端，所有玩家打开游戏时，先拉云端的排行榜，云端拉不到就降级用本地存储。

核心是「本地先写、云端后同步」+「读时云端优先、本地降级」这套双层架构：

```javascript
// 配置：JSONBin 的 Bin ID 和写权限 Key
const BIN_ID = '6a2ad19bf5f4af5e29dfe98e';
const WRITE_KEY = '$2a$10$xxxxxxxxxxxxx'; // 只有写的时候才用
const READ_URL  = `https://api.jsonbin.io/v3/b/${BIN_ID}/latest`;
const WRITE_URL = `https://api.jsonbin.io/v3/b/${BIN_ID}`;

// 1. 读取：先云端（拉全班最新数据），5 秒超时降级本地
async function getLB() {
  try {
    const ctl = new AbortController();
    const timer = setTimeout(() => ctl.abort(), 5000);
    const res = await fetch(READ_URL, { signal: ctl.signal });
    clearTimeout(timer);
    const data = await res.json();
    return data.record || [];   // ✅ 拿到云端数据
  } catch (e) {
    return JSON.parse(localStorage.getItem('lb') || '[]');  // ⬇️ 降级本地
  }
}

// 2. 写入：本地立刻存（不丢），再异步推云端
async function saveLB(entry) {
  // 2.1 本地先写（哪怕断网也不会丢成绩）
  const local = JSON.parse(localStorage.getItem('lb') || '[]');
  local.push(entry);
  local.sort((a, b) => b.score - a.score);
  localStorage.setItem('lb', JSON.stringify(local.slice(0, 20)));

  // 2.2 异步推云端：拉一份云端 + 合并去重 + 截前 50 名 + PUT 回去
  try {
    const cloud = await getLB();
    const merged = [...cloud, entry]
      .filter((v, i, a) => a.findIndex(x => x.name === v.name) === i)
      .sort((a, b) => b.score - a.score)
      .slice(0, 50);
    await fetch(WRITE_URL, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json', 'X-Access-Key': WRITE_KEY },
      body: JSON.stringify(merged)
    });
  } catch (e) {
    console.warn('云端同步失败，本地已存:', e);
  }
}

// 3. 使用：玩家提交成绩
saveLB({ name: '小明', score: 280, ts: Date.now() });
```

**教训：技术选型要服从"团队能力"，不要为了"看起来高级"去选一个你们 hold 不住的技术。**

---

## 五、我们做了一个"反传统"的产品决策：屈原不跳江

传统的端午故事里，屈原是投江而死的。

但在我们游戏里，屈原**没有跳江**。

他穿越到了现代，站在江边，看着来来往往的龙舟和粽子，对玩家说：

> "老夫等了两千年，终于等到了懂我的人。"

为什么这么做？因为我们觉得，端午不应该只是一个"哀悼"的故事。它更应该是一个"传承"的故事——屈原留下的，不是一段投江的悲剧，而是《离骚》、《天问》、九歌、楚辞，是整个楚文化的精神火种。

我们要传递的不是悲伤，而是"文化在新一代人手里继续发光"的希望。

这才是"正能量"。

---

## 六、最小也最大的项目

做完后我们回头看，这个游戏其实非常"小"：

- 一个 HTML 文件，1300 行
- 没有后端，没有数据库（云端排行榜用的是第三方服务）
- 一个人一周就能写完
- 服务器用的是 GitHub Pages（免费）

但它又非常"大"：

- 承载了一整个端午文化的科普
- 让 23 物流 2 班的同学有了一个"作品"可以拿出来
- 让我们五个人，从"只会做物流项目"变成了"也能做文化传播项目"
- 也许——只是也许——会让某个打开这个游戏的陌生人，对端午多了一点点喜欢

这就是我们理解的"最小也最大"。

**不是项目越大越好，而是项目越有意义越好。**

---

## 七、最后

如果你读到了这里，谢谢你。

我们没有想改变世界，我们只是想——在这个端午，用我们这一代人的方式，给传统文化发一条"消息"：

> 我们在，我们懂，我们会继续传下去。

---

📲 **扫码体验游戏**：[端午文化探险 H5](https://daka-agent.github.io/DragonBoatFestival/duanwu.html)

🏆 **通关后记得生成你的文化海报**，发朋友圈 @ 五四青年团队，让更多人看到你！

—— 五四青年团队 · 23物流2班 · 端午献礼
